# Changelog

<!--
    File: Project changelog.
    Intent: Track user-facing changes for releases.
-->

## Unreleased

- Added Windows CLI for running and installed Electron app detection.
- Added text, `--paths`, and `--json` output modes.
- Added NTFS MFT/USN-based installed-app cache refresh.
- Added detection for `resources\app.asar` and unpacked `resources\app\package.json` layouts.
- Added cache at `%LOCALAPPDATA%\electron-detector\cache.json`.
